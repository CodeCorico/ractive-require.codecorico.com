(function() {
  'use strict';

  window.Ractive.controller('user-profile', function(component, data, el, config, done) {

    var UserProfile = component({
      data: data
    });

    done();
  });

})();
